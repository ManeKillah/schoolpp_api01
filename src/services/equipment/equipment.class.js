const { Service } = require('feathers-objection');

exports.Equipment = class Equipment extends Service {
  constructor(options) {
    const { Model, ...otherOptions } = options;

    super({
      ...otherOptions,
      model: Model,
    });
  }
};
